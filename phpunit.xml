<?xml version="1.0" encoding="UTF-8"?>
<!-- https://phpunit.readthedocs.io/fr/latest/configuration.html s-->
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="https://schema.phpunit.de/10.5/phpunit.xsd" backupGlobals="false" colors="true" bootstrap="tests/bootstrap.php" cacheResult="false" cacheDirectory=".phpunit.cache">
  <php>
    <ini name="display_errors" value="1"/>
    <ini name="error_reporting" value="-1"/>
    <server name="APP_ENV" value="test" force="true"/>
    <server name="SHELL_VERBOSITY" value="-1"/>
    <server name="SYMFONY_PHPUNIT_REMOVE" value=""/>
    <server name="SYMFONY_PHPUNIT_VERSION" value="9.5"/>
    <env name="SYMFONY_DEPRECATIONS_HELPER" value="disabled"/>
  </php>
  <testsuites>
    <testsuite name="Project Test Suite">
      <directory>tests</directory>
    </testsuite>
  </testsuites>
  <!--
    Code the coverage reports are computed from. Without it PHPUnit raises the "No filter is
    configured, code coverage will not be processed" runner warning, which makes it exit with 1:
    the workflows ask for a clover report and upload it to Codecov, so the whole suite was failing
    on a configuration gap rather than on a test.
  -->
  <source>
    <include>
      <directory>application</directory>
      <directory>library</directory>
      <directory>scripts</directory>
    </include>
  </source>
  <logging>
    <junit outputFile="build/junit.xml"/>
  </logging>
  <!-- Run `composer require symfony/panther` before enabling this extension -->
  <!--
    <extensions>
        <extension class="Symfony\Component\Panther\ServerExtension" />
    </extensions>
    -->
</phpunit>
