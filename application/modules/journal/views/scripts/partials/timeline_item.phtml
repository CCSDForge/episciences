<?php
$comment = $this->comment;
$id = $comment['PCID'] ?? null;
if (!$id) {
    return;
}

// Ensure $isAuthor is defined (default to false if not passed to partial)
$isAuthor = $isAuthor ?? false;

// Ensure $authorReplyForms is available (try passed variable first, then view variable)
$authorReplyForms = $authorReplyForms ?? ($this->authorReplyForms ?? []);
?>

<article class="timeline-item"
         aria-labelledby="comment-header-<?= $id ?>">

    <!-- Timeline marker with user photo -->
    <div class="timeline-marker" aria-hidden="true">
        <?php
        // Define avatar colors based on comment type
        $avatarColor = null;
        if (isset($comment['TYPE'])) {
            if ($comment['TYPE'] == 22) { // TYPE_AUTHOR_TO_EDITOR
                $avatarColor = 'FFC107'; // Yellow for authors
            } elseif ($comment['TYPE'] == 23) { // TYPE_EDITOR_TO_AUTHOR_RESPONSE
                $avatarColor = '2196F3'; // Blue for editors
            }
        }

        $avatar = $this->userAvatar($comment, 'initials');
        if ($avatarColor) {
            $avatar->setBackgroundColor($avatarColor);
        }
        echo $avatar;
        ?>
    </div>

    <!-- Comment content card -->
    <div class="timeline-content">

        <!-- Header: date + author -->
        <header class="timeline-comment-header" id="comment-header-<?= $id ?>">
            <time class="comment-date" datetime="<?= date('c', strtotime($comment['WHEN'])) ?>">
                <?= $this->Date($comment['WHEN'], null, Zend_Date::DATE_LONG . ' - ' . Zend_Date::TIME_SHORT) ?>
            </time>
            <i class="fa-solid fa-user" aria-hidden="true"></i>&nbsp;<?= $this->translate('De : ') ?>
            <span class="comment-author">

                            <strong>
                                <?= ((int)$comment['UID'] === Episciences_Auth::getUid()) ? $this->translate('Vous') : $this->escape($comment['SCREEN_NAME']) ?>
                            </strong>
                            </span>
        </header>

        <!-- Comment message -->
        <div class="timeline-comment-message">
            <?= Episciences_Tools::formatText(Episciences_Tools::epi_html_decode($comment['MESSAGE'])) ?>
        </div>

        <?php
        // Author can reply to editor responses
        if (
                isset($comment['TYPE']) &&
                (int)$comment['TYPE'] === Episciences_CommentsManager::TYPE_EDITOR_TO_AUTHOR_RESPONSE &&
                $isAuthor &&
                isset($authorReplyForms[$comment['PCID']])
        ) :
            ?>
            <!-- Author reply action -->
            <div class="timeline-comment-actions">
                <button type="button"
                        class="btn btn-primary btn-xs toggle-reply-form"
                        data-reply-form-id="author-reply-form-<?= $comment['PCID']; ?>">
                    <i class="fa fa-reply"></i>
                    <?= $this->translate('Répondre'); ?>
                </button>
            </div>

            <!-- Author reply form -->
            <div class="reply-form-container"
                 id="author-reply-form-<?= $comment['PCID']; ?>"
                 style="display: none; margin-top: 10px;">

                <h5><?= $this->translate('Répondre'); ?></h5>

                <?php
                $form = $authorReplyForms[$comment['PCID']];
                $form->setAttrib('class', 'author-reply-form');
                echo $form;
                ?>
            </div>
        <?php endif; ?>

        <!-- Attached file (if present) -->
        <?php if ($comment['FILE']) : ?>
            <div class="timeline-comment-attachment">
                <div>
                    <i class="glyphicon glyphicon-paperclip" aria-hidden="true"></i> <a
                            href="/docfiles/comments/<?= $comment['DOCID'] . '/' . $comment['FILE'] ?>"
                            target="_blank"
                            rel="noopener noreferrer"
                            aria-label="<?= $this->translate('Télécharger le fichier'); ?> <?= $this->escape($comment['FILE']) ?>">
                        <?= $this->escape($comment['FILE']) ?>
                    </a>
                </div>
            </div>
        <?php endif; ?>

        <!-- Delete button (always visible for own comments) -->
        <div class="remove-file-btn">
            <?= $this->partial('partials/remove_file_comment.phtml', ['comment' => $comment]) ?>
        </div>

        <!-- Optional actions (edit, reply, etc.) -->
        <?php if (isset($this->actions) && $this->actions) : ?>
            <div class="timeline-comment-actions">
                <?= $this->actions ?>
            </div>
        <?php endif; ?>

    </div>

</article>