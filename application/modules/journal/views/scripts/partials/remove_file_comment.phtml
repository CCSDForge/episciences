<?php if ($this->comment['UID'] == Episciences_Auth::getUid()): ?>
<?php // le champ file de la table paperComments est encoder en JSON si plusieurs fichiers sont attachés à ce commentaire ?>
    <?php $file = $this->fileName ?? $this->comment['FILE']; ?>
    <?php
    $file_delete_url = sprintf('%scomments/removefilecomment/pcid/%s/docid/%s/file/%s', PREFIX_URL, $this->comment['PCID'], $this->comment['DOCID'], $file) ?>
    <a class="btn btn-danger btn-xs pull-right" style="margin-bottom:5px;" onclick="confirmDeleteFile('<?= $file_delete_url ?>')">
        <span class="glyphicon glyphicon-trash" style="margin-right: 5px;"></span>
        <?= $this->translate('Supprimer')?>
    </a>
    <script>
        function confirmDeleteFile(href) {
            if (confirm(translate('Voulez-vous supprimer ce fichier ?'))) {
                location.href = href;
            }
        }
    </script>
<?php else:?>
<br>
<?php endif; ?>
