<div id="showResult" style="display:none"></div>
<?= $this->form ?>
<script>
    isFromZSubmit = <?= $this->isFromZSubmit ?>; // declared in /paper/view.phtml
    let src_url = "/js/submit/functions.js?_=v<?= APPLICATION_VERSION ?>";
    let visual_delete_url = "/js/common/visualDeleteFile.js?_=v<?= APPLICATION_VERSION ?>";
    let zSubmitElement_url = "/js/submit/insertZSubmitElement.js?_=v<?= APPLICATION_VERSION ?>";

    $.getScript(src_url);
    $.getScript(visual_delete_url);

    $.getScript(zSubmitElement_url)
        .done(function () {
            if (!isFromZSubmit && $('#search_doc-repoId').val() === '<?= $this->zenodoRepoId ?>') {
                if(zSubmitStatus){ // initialised in /paper/view.phtml
                    insertZSubmitElement('<?= $this->zSubmitUrl?>');
                }
            }
        })
        .fail(function (jqxhr, settings, exception) {
            console.log(exception);
        });

</script>