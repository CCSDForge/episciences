// jQuery File Upload (basic plugin) 10.32.0, previously loaded from cdnjs (VENDOR_JQUERY_FILE_UPLOAD),
// now self-hosted via webpack. Attaches $.fn.fileupload to the global jQuery (see addExternals in
// webpack.config.js). Only the basic plugin is imported, matching the single file previously loaded
// (no UI/process/validate/image/audio/video extensions — public/js/library/es.fileupload.js only uses
// the basic add/done/progress/fail callbacks).
import 'blueimp-file-upload/js/jquery.fileupload.js';
