// jQuery UI 1.12.1 "base" theme CSS, previously the "flick" theme loaded from cdnjs
// (VENDOR_JQUERY_UI_THEME_CSS), now self-hosted via webpack. "flick" isn't published in the jquery-ui
// npm package (only "base" ships), so the datepicker/autocomplete widgets now use the base theme.
// Only used on pages with a jQuery UI datepicker or autocomplete widget.
import 'jquery-ui/themes/base/all.css';
