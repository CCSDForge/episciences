// SortableJS 1.15.7, previously loaded from jsdelivr (VENDOR_SORTABLE_JS), now self-hosted via webpack.
// Standalone (no jQuery dependency). Exposed as window.Sortable, used as a bare global in page scripts
// (website/menu, website/header, volume/list, section/list).
import Sortable from 'sortablejs';

window.Sortable = Sortable;
